## This commit
Rebuilt for round 8 (shorthand removal); see stack-wide context in
sibling commits' notes.

## Round 8 addendum: --fk/--fake shorthand removed
Review comment: 'Dont need this shorthand.' Removed the --fk/--fake
boolean addoption; the backend is selected only via --trace-server=fake
(--ch/--clickhouse shorthand is pre-existing on the base, untouched).
Cascade: get_trace_server_flag lost its --fake branch;
NOT_CLICKHOUSE_BACKEND (tests/trace/util.py) no longer references
config.getoption('--fake') — IMPORTANT: it would raise ValueError at
collection if it referenced a now-unregistered option; AGENTS.md shorthand
sentence dropped. Verified: skipif gates still fire under
--trace-server=fake (35 skips across spot files), zero skips on ClickHouse
(25 passed), full fake shards 1203/115/0 and 1664/44/1 (flask,
pre-existing).

## PR Description / Lint Follow-up Context
The user asked to update PR descriptions to match the corrected test ownership. While doing that, `nox --no-install -e lint` exposed that `weave/trace_server/in_memory_trace_server.py` used `NotFoundError` without importing it after the previous stack rewrite. The missing import was amended into `andrew/fake-ts-02-call-writes`, and descendants were restacked. This preserves the corrected PR boundary: the root PR owns the implementation import and write behavior, while call lifecycle test coverage remains in `andrew/fake-ts-calls-read`.
Copied from prior rewritten commit 0169b08625ec after the NotFoundError import fix/restack.
