## Split of Google ADK plugin-core — slice 2/3 (model/chat spans)
See slice 1 (andrew/google-adk-01-plugin-run) and tag bak-adk-old-plugincore for context + full original history.

Adds: model callbacks (beforeModel/afterModel/onModelError → nested chat spans), setLlmRequest/ResponseAttributes + toolDefinitions + systemInstructionParts, childContext, errorTypeOf/errorMessageOf, closeShortCircuitedModelSpan + modelSpanKeyForEvent, InvocationState.modelSpans + finishInvocation model loop + onEvent short-circuit, semconv +TOOL_DEFINITIONS, model types. Also adds tool-callback NO-OP STUBS (return undefined) so the plugin structurally satisfies ADK's BasePlugin type for the real-runner model tests (records-model-errors, cached) — stub bodies are filled in slice 3.

Built: restore full old plugin-core source, strip ONLY tool code (run+model lines stay byte-identical to the original). Verified 17/17 jest + lint clean.

## Session addendum — move ADK GoogleGenAI exclusion into model slice
User clarified every PR must be standalone. The ADK-internal GoogleGenAI exclusion originally lived in the later genai-exclusion slice, but model/chat tracing starts in this slice. Without the exclusion here, this PR alone can double-record ADK Gemini model calls: WeaveAdkPlugin emits chat spans with usage while the generic GoogleGenAI wrapper can also wrap ADK's internal GoogleGenAI client and emit orphan/double-counted traces.

Moved the x-goog-api-client google-adk/ marker exclusion from the later genai-exclusion branch into this model slice, along with the fake GoogleGenAI regression test. This makes the first PR that introduces model/chat spans standalone and prevents token double-counting at that point in the stack. The later genai-exclusion branch should become empty/drop after restacking.

This commit also inherits the slice-1 standalone runner test/stubs from the restack conflict resolution.

Verification for this amendment:
- cd sdks/node && PATH=/opt/homebrew/opt/node/bin:$PATH pnpm test -- --runInBand src/__tests__/integrations/googleAdk.test.ts (19 passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec eslint src/integrations/googleAdk.ts src/integrations/googleAdk.types.ts src/integrations/googleGenAI.ts src/__tests__/integrations/googleAdk.test.ts
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec prettier --check src/integrations/googleAdk.ts src/integrations/googleAdk.types.ts src/integrations/googleGenAI.ts src/__tests__/integrations/googleAdk.test.ts

No push performed. Notes preserved/copied by Graphite where available; this addendum records the stack-boundary change.

---

## Session addendum - final branch-green verification after base lint cleanup
The user asked to run each PR in the stack and confirm it is green. Initial stack-owned checks passed, but full sdks/node npm run lint failed on every branch due the same @typescript-eslint/consistent-type-imports errors in unrelated files. A mechanical import-type cleanup was amended into the bottom branch so every surviving PR can pass full package lint. This branch was restacked after that base cleanup and then rerun.

Final verification for this branch:
- cd sdks/node && PATH=/opt/homebrew/opt/node/bin:$PATH pnpm test -- --runInBand src/__tests__/integrations/googleAdk.test.ts (19 passed, 19 total)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec eslint changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec prettier --check changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run build (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run lint (passed)

No push performed.

===================================================================
## ADDENDUM — type-precision pass (later session)
===================================================================

## Actions (slice 02 — owns LlmRequest/Response, config, model lifecycle)
googleAdk.types.ts:
- AdkLlmResponse.content: dropped `| null` (adk LlmResponse.content is
  `Content | undefined`, never null).
- AdkLlmResponse.finishReason: unknown -> string (FinishReason string enum).
- REMOVED phantom fields interactionId + modelVersion. These do NOT exist on
  the TS @google/adk LlmResponse (nor anywhere in @google/genai —
  interactionId: 0 hits; modelVersion is on GenerateContentResponse only, not
  surfaced onto LlmResponse). They were transliterated 1:1 from the Python
  integration's llm_response.interaction_id / model_version, which DO exist on
  Python ADK. On the TS surface both read branches were permanently dead (zero
  test coverage). User approved removal (option 1 of remove / wire-up / keep).
- Removed dead AdkLlmResponse.turnComplete (impl keys off `partial`, never
  reads turnComplete; the test sets it on the REAL LlmResponse, unaffected).
- AdkLlmRequest.contents -> required (adk LlmRequest.contents: Content[]).
- Removed dead AdkGenerateContentConfig.responseSchema (never read).
googleAdk.ts:
- setLlmResponseAttributes: removed the interactionId/modelVersion branches and
  the now-unused ATTR_GEN_AI_RESPONSE_ID / ATTR_GEN_AI_RESPONSE_MODEL imports.
- setLlmRequestAttributes: llmRequest?.config -> llmRequest.config; dropped the
  `Array.isArray(llmRequest?.contents) ? ... : []` guard (contents now required).
- before/after/onError ModelCallback: dropped `ctx ? ...get... : undefined`
  ternaries and `?.` on the ADK-guaranteed callbackContext/llmRequest/llmResponse;
  `params.llmRequest ?? {}` -> `params.llmRequest`; afterModelCallback unwrapped
  the always-true `if (response != null)`.

## Hints for Future Agents
- If a future ADK version DOES surface a response id/model on LlmResponse, the
  correct genai field is `responseId` (on GenerateContentResponse), NOT
  `interactionId`. Wiring it would require the plugin to receive the underlying
  GenerateContentResponse, which it currently does not.

## Verified
tsc esm+cjs PASS, prettier clean, slice-02 jest 19/19.

---
## Session addendum — review-comment pass: toMatchObject + optional tightening (2026-06-23)

User left two stack-wide review comments and approved a full sweep ("yes, and
tighten comment 1 wherever you can. Do this up and down the stack").

REVIEW VERDICT on comment 1 (make wrongly-optional things required):
- Only ONE genuine tightening exists in the whole stack: functionCallResponse(usage)
  in slice 03 (its sole caller always passes usage). Done in slice 03.
- textResponse(usage?) STAYS optional: 5 of 7 callers omit usage.
- googleAdk.types.ts fields STAY optional by design — they are structural duck
  types of @google/adk/@google/genai objects, optional so the real (optional)
  ADK types remain structurally assignable; always-present fields (name,
  session.id, invocationId, agentName, contents) were already required.

THIS SLICE (02, model/chat): comment 2 — converted 5 lone span-attribute
assertions to expect(span.attributes).toMatchObject({...}):
cached-response OUTPUT_MESSAGES, dangling-spans interrupted, capture=false
REQUEST_MODEL, provider test.each PROVIDER_NAME, capture test.each
OUTPUT_MESSAGES (the line the user flagged). expect.stringContaining preserves
substring strength. Left .toBeUndefined() absence checks as-is (toMatchObject
asserts presence, not absence).

Validated at stack tip: eslint clean, typecheck:esm clean, 26 ADK jest tests pass.

---
## Session addendum — literal OTEL keys in tests (2026-06-23)

Per review feedback ("use the literal OTEL key instead of the const ... easier
to read in the test"), googleAdk.test.ts now uses literal OTEL semconv strings
(e.g. 'gen_ai.output.messages', 'gen_ai.tool.call.id') in place of the
ATTR_GEN_AI_* consts imported from ../../genai/semconv; the now-unused semconv
import was removed. TEST-ONLY — production googleAdk.ts still imports the consts.

Applied uniformly across the whole ADK slice stack (01/02/03/05/06) via a
deterministic, idempotent transform (.context/relit.mjs, gitignored): object
keys [CONST]: -> 'lit':, property access x[CONST] -> x['lit']. Each upstack
restack hit the test file and was resolved with `git checkout --theirs` +
re-running the transform, so every slice's full file ends up literal-ized.
Notes survived because these restacks took git's conflict path (gt's
clean-restack path would have dropped them).

Validated: eslint clean, typecheck:esm clean; jest green (tip 26, slice 03 22,
slice 01 5).

---
## Session addendum — review pass: drop !, combine asserts (2026-06-23)

Three review comments; user approved 1+2 and asked to use toMatchObject for 3.

Comment 1 (unnecessary `root!`): added a `requireSpan(span | undefined):
ReadableSpan` test helper (throws if missing, narrows the type) so tests stop
needing the `!` non-null assertion after `.find()` — `expect(x).toBeDefined()`
does NOT narrow the TS type, which is why `!` was there. THIS SLICE: model-error
test now `requireSpan(byOperation(...).find(...))`, dropped `!` and the redundant
toBeDefined.

Comment 2 (combine): cached-response chat-span asserts folded into one
`expect(chatSpan).toMatchObject({status:{code:0}, attributes:{...output...}})`.
`status.code).not.toBe(2)` became `code:0` (UNSET) — a tightening, correct since
the plugin only ever sets ERROR. The `interrupted` check stays separate
(toMatchObject asserts presence, not absence).

Comment 3 (genai-exclusion generateContent.name): tried toMatchObject but jest
REJECTS functions ("received value must be a non-null object"), so kept
`expect(fn.name).toBe('generateContent')`. Reported the limitation to the user.

Validated: eslint clean, typecheck:esm clean, jest green (slice 02 19, tip 26).
Kept LOCAL/unpushed, bundled with the prior literal-key change.

===================================================================
## ADDENDUM — duck-type file deleted; migrate to provided types (later session)
===================================================================

## Conversation
Reviewer @drtangible left a GitHub comment on PR #7301 against
sdks/node/src/integrations/googleAdk.types.ts (lines 5-6): "This file no longer
exists [in the openai pattern], and we've started moving towards using provided
types where possible. Guessing we could do the same with this library, assuming
they export what we need." The user directed: rebase onto origin/master, prefer
the types ADK exports, keep local types only for shapes it does NOT export, and
follow how the OpenAI integration does it (openai.agent.ts / openai-agents/
import provided types directly and have NO companion *.types.ts duck-type file).
User then clarified "follow what Dan suggested" = option B: DELETE
googleAdk.types.ts entirely.

## Actions
1. Rebased the whole 5-slice stack onto origin/master 841d7f879e (gt sync
   --no-restack FF'd the worktree-shared master, then gt restack the slices).
2. Deleted sdks/node/src/integrations/googleAdk.types.ts in every slice.
3. In googleAdk.ts, replaced `import type {Adk*} from './googleAdk.types'` with
   provided types, aliased to keep the Adk* names and dodge global collisions
   (Event/Content/Part/Blob):
     - from '@google/adk':   BaseAgent, BaseTool, Event, InvocationContext,
                             LlmRequest, LlmResponse   (as Adk*)
     - from '@google/genai': Content, FunctionDeclaration,
                             GenerateContentConfig, Part   (as Adk*)
   Verified against the installed .d.ts that @google/adk exports the ADK types
   but does NOT re-export the genai types, so the @google/genai import is
   genuinely required (can't fold everything into one @google/adk import).
4. Inlined the shapes neither package exports, as local interfaces with
   docstrings explaining why: AdkCallbackContext / AdkToolContext (invocationId
   & agentName are getters on ADK's non-exported ReadonlyContext; ToolContext
   adds functionCallId), AdkPluginManager / AdkRunnerLike (Runner/PluginManager
   internals the hook touches). Each slice carries only the gaps it uses.
5. Tests: the partial mock object literals the tests pass to plugin callbacks no
   longer satisfy the strict provided types, so added `as any` to the
   event/llmRequest/tool/toolContext mocks (test-only; behavior unchanged).
6. The rebased master tightened eslint (@typescript-eslint/no-require-imports);
   converted the test's setGlobalClient `require` to a top-level import.

Migration was applied deterministically: .context/migrate-adk-types.mjs (import
rewrite + gap-interface emission + file delete) and .context/cast-mocks.mjs
(brace-matched `as any` on mock literals), cascaded bottom-up via gt restack
with take-theirs conflict resolution per slice.

## Validation (this slice, independent)
- src/integrations/googleAdk.types.ts gone; googleAdk.ts imports provided types
- npm run typecheck:esm: clean
- eslint googleAdk.ts + googleAdk.test.ts: clean
- jest googleAdk.test.ts: all passing

## Hints for Future Agents
- `import type` is erased at runtime, so @google/genai resolves transitively
  through @google/adk; jest/runtime never need it and typecheck passes because
  it is flat in node_modules. DEFERRED (flagged to user, intentionally NOT
  done): promoting @google/genai to a direct devDep for parity with how
  openai-agents declares both @openai/agents and openai. Held back to avoid
  noisy package-lock churn in the requested push; it is a clean 1-line
  package.json + lockfile follow-up. NB @google/adk@^1.2.0 is already on master
  (no ADK slice touches package.json).
- gt's clean-restack path DROPS git notes; these notes were recovered from the
  pre-migration SHAs via reflog and `git notes copy -f <old> <new>`, then this
  addendum appended.

## Errors & Mistakes
- migrate-adk-types.mjs first used a greedy `[\s\S]*?` regex that matched from
  the FIRST `import type {` (the @opentelemetry block), mangling unrelated
  imports and dropping AdkBaseAgent. Fixed to
  `import type \{([^}]*)\} from '\./googleAdk\.types';` ([^}]* can't cross
  another import's braces). Caught only because source typecheck was rerun —
  jest alone would have missed it; always run typecheck, not just tests.
- cast-mocks.mjs missed `const tool = {...}` variable mocks (it matched only
  `tool: {`); handled those by hand.

## Addendum — normalize import order (externals grouped at top)
Reviewer flagged that the @google/adk import block "moved" between slices (churn
between PRs) and asked to check all import moves. Audit found two inconsistencies
at the 01→02 boundary, both now fixed to ONE convention — external imports
(@opentelemetry, @google/adk, @google/genai, zod) grouped at the TOP, relative
imports below — in BOTH googleAdk.ts and googleAdk.test.ts, every slice:
- SOURCE: slice 01 had @google/* at the bottom (where the old ./googleAdk.types
  relative import used to sit before the type migration); moved to the top to
  match 02-06.
- TEST: slices 02-06 had @google/adk at the bottom; moved to the top to match
  slice 01.
No linter enforces import order here (there is no import/order rule, and
`eslint --fix` is a no-op on imports), so this is purely churn-reduction for
cleaner per-PR diffs — not a lint fix.

Restack mechanics: slice 01 amended (source only); 02-06 restacked. Conflicts hit
on 02 (symbol-set merge: slice 02 adds LlmRequest/LlmResponse/FunctionDeclaration/
GenerateContentConfig) and on 06 (the test import block tangled into DUPLICATE
relative imports via git's auto-merge — clientApi appeared 3x). Both resolved to
the canonical layout. 03/05 auto-merged clean (verified not tangled). Confirmed
clientApi imported exactly once per test file and all slices green
(typecheck/eslint/jest 5/19/22/24/26).

GOTCHA for future agents: reordering imports across a stacked PR is conflict-prone
— moving a block in a lower slice collides with later slices that add symbols to
it or add neighboring imports, and a "clean" auto-merge can SILENTLY duplicate
imports (slice 06 here). Always re-validate each slice (typecheck + a dup-count
grep), don't trust a clean restack.

## Addendum — googleGenAI.ts wrap-case comment
Reviewer (re isAdkInternalClientOptions, googleGenAI.ts:256) asked to spell out the else/wrap case in a comment. Added at the call site: "// The user is using GoogleGenAI directly, not through ADK: wrap it." isAdkInternalClientOptions is true ONLY for ADK-internal clients (skipped — WeaveAdkPlugin already traces them as chat spans, so wrapping would double-count); the wrap branch is standalone GenAI use. Comment-only; slice 02 amended, 03/05/06 clean-restacked (notes re-copied).

## Addendum — use existing semconv consts for gen_ai.request.* attrs
Reviewer (googleAdk.ts:375) asked to pull the inline 'gen_ai.request.*' attribute-key string literals into the semconv file / use consts. Found all 8 (temperature, top_p, max_tokens, frequency_penalty, presence_penalty, seed, stop_sequences, choice.count) ALREADY exist as ATTR_GEN_AI_REQUEST_* consts in src/genai/semconv.ts with byte-identical values — setLlmRequestAttributes was just inlining the literals instead of importing them. Added the 8 imports + replaced the literals; no semconv changes needed. Matches how the rest of the file already uses ATTR_GEN_AI_* consts. Slice 02; jest 19/19.

## Addendum — rebased onto origin/master 58285d4c6b
Restacked the whole stack onto latest origin/master (was 841d7f879e). The 3 new commits are Python-only (eval predict-token-total #7364, trace-server owns AgentSpanOpName #7357, prompt/type_wrappers decouple) — zero sdks/node overlap, so the rebase was conflict-free and slice content is unchanged (re-parent only). Tip green: typecheck/eslint/jest 26/26.
