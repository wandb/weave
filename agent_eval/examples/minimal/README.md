# Minimal Evaluation Example

The simplest possible `agent_eval` example to get started.

## What it does

Tests a basic skill that creates a text file with specified content.

## Running

```bash
agent-eval run eval.yaml
```

## Scoring

Checks:
1. `output.txt` file exists
2. File contains "Hello"
