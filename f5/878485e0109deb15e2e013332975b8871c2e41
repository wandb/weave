## Conversation
Slice 4/4 of the REFRAMED Google ADK stack (each PR standalone + tested +
docs). Final slice: module-hook auto-instrumentation + example + remaining
AGENTS.md bullets. See slice 1 note for reframe context.

## Actions
Restored the ENTIRE old-stack final tree (andrew/adk-ts-04-docs: AGENTS.md +
sdks/node) onto the slice-3 base, then re-applied the \0-escape fix to
syntheticQueueKey. Verified `git diff --cached andrew/adk-ts-04-docs` shows
EXACTLY one line (the NUL -> \0 escape) — the four-slice stack reproduces
the old stack's end state byte-for-byte modulo that intentional fix. This
slice's diff therefore contains: the auto-instrumentation section of
googleAdk.ts (globalSingleton holder, getSharedPlugin, ensurePluginRegistered,
patchRunnerClass, commonPatchGoogleADK, instrumentGoogleADK), ADK_VERSION_RANGE
/ ADK_CJS_SUBPATH consts, the Automatic-usage header block, AdkPluginManager/
AdkRunnerLike types, hooks.ts registration, the auto-registration test (+
commonPatchGoogleADK import), examples/googleAdkAgent.ts, and the AGENTS.md
hooks/jest bullets.

## Errors & Mistakes
None in this slice.

## Dead Ends
None (see old-stack notes for the OTel-design rejection and jest/require-hook
findings).

## Hints for Future Agents
- typecheck:examples fails on master with a pre-existing error in
  examples/streamFunctionCalls.ts (openai v6 type drift); googleAdkAgent.ts
  compiles clean.
- Real loader-hook behavior can only be tested in the hostApps jest project
  or e2e hosts; never assert require-hook effects inside default-project
  jest tests (jest bypasses Module.prototype.require).
- Old stack (andrew/adk-ts-01..04, draft PRs #7135-#7138) left intact for
  comparison; .context/reframe/PLAN.md documents the slice mapping and
  .context/notes-backup/ holds the old notes.

## Codebase Discoveries
None new.

## Open Questions
- Real-Gemini e2e still unrun (gcloud reauth needed); scripted-BaseLlm e2e +
  prod-verified error path stand in. Rerun recipe in the old adk-ts-04-docs
  note.
- weave-docs site (guides/integrations) not updated; follow-up if the
  integration should be documented publicly.

---

# ADDENDUM: non-ADK-user runtime impact audit (2026-06-10)

User asked to verify the integration has no runtime impact on weave users
who do not use ADK. Findings (no fixes needed — design is fully lazy):

- Registration (instrumentGoogleADK at import weave): two in-memory Map
  pushes in instrumentations.ts. No I/O, no module resolution, no semver
  eval. NODE_DEBUG=module on the built dist shows ZERO '@google/adk'
  lookups during import (the only 'adk' matches are weave's own
  googleAdk.js file loading).
- CJS hot path: Module.prototype.require patch is PRE-EXISTING weave core
  (utils/commonJSLoader.ts); per-require cost is one O(1) Map.has on
  '<name>@<subPath>'. The expensive branch (requirePackageJson + semifies +
  hook) runs only when '@google/adk@dist/cjs/index.js' is actually required.
- ESM: only for hosts that opt into --import=weave/instrument; adds one
  import-in-the-middle Hook(['@google/adk']) — parity with the existing 5
  integrations.
- Import-time A/B (12 runs each, dist built at stack merge-base 0b0ea6e1a9
  vs stack top): median 115.3ms vs 105.9ms, mins 90.6 vs 90.0 — delta below
  noise.
- Process listeners after import: beforeExit 0 / exit 0, identical to base.
  registerBeforeExitHookOnce only fires on the first traced ADK invocation.
- genai constructor check (isAdkInternalClientOptions): runs only for
  @google/genai users, O(#headers) per client construction; non-ADK clients
  keep being wrapped identically (51 genai tests green).
- Dependencies: runtime "dependencies" unchanged; @google/adk + lodash are
  devDependencies; the lodash-es->lodash mapping is jest-only.
- No-ADK e2e: hostApps fixtures install the packed SDK into hosts WITHOUT
  @google/adk and pass (cjs-basic + esm-basic, run in the full suite).

Bench method: git worktree --detach at merge-base in /tmp, symlinked
node_modules, npm run build both sides, 12x require() timing each.

---

# ADDENDUM: offline smoke script (2026-06-10)

User asked for a minimal locally-runnable verification script, run and
verified by the agent. Added examples/googleAdkSmoke.ts: no keys, no
network — ScriptedLlm (BaseLlm subclass) plays Gemini, an inline node http
server plays the trace server recording /call/upsert_batch bodies, and the
script self-checks 11 properties (auto-registration with NO plugin passed,
op counts, root->invoke_agent->{call_llm,execute_tool} parenting, matching
ends, per-model usage on call_llm ends, final answer as invocation output),
exiting 0/1. Run: cd sdks/node && npm run build && npx tsx
examples/googleAdkSmoke.ts.

Verified PASS (all 11 checks; ADK logs "Plugin 'weave' registered.").

Gotchas discovered while writing it:
- The workspace had been switched back to slice 3 (no auto-instrumentation)
  between sessions, so the first smoke run failed auto-registration and
  recorded zero calls — NOT an integration bug. With slice 4 checked out it
  passes, including under npx tsx (tsx resolves 'weave' to src via tsconfig
  paths and routes @google/adk through the patched CJS require, so the
  module hook fires).
- Slices 1-2 had been rebased onto newer master outside this session;
  gt restack rebased 3-4 on top (no conflicts) and notes were re-copied to
  the new SHAs.
- bare-specifier resolution happens from the script's directory, so the
  smoke script must live inside sdks/node (e.g. examples/) to resolve
  weave/@google/adk.
- init('entity/project') (with a slash) performs no wandb-api call, so the
  inline server only needs to answer /call/upsert_batch (and 200 anything
  else) — batch items are {mode:'start'|'end', req:{start|end:{...}}}.

---

# CORRECTION to the runtime-impact audit addendum

The original audit's EMPIRICAL runs unknowingly used a slice-3 build: the
workspace had been switched to andrew/google-adk-03-agent-hierarchy between
sessions, so that dist had no hooks registration / auto-instrumentation
(registry showed 8 keys, no adk). The code-read conclusions were unaffected.
Re-run on the real slice-4 tip (4fde323b1c, after restack onto newer
master):
- CJS registry has '@google/adk@dist/cjs/index.js', ESM registry has
  '@google/adk' — and import still performs ZERO '@google/adk' resolution
  attempts (NODE_DEBUG=module count 0).
- beforeExit/exit listeners after import: 0 / 0.
- Clean import A/B at the new merge-base (35ff130437) vs tip, 12 runs,
  same-moment machine: base median 267.6ms / min 144.5 vs tip 237.8 / min
  147.9 — minimums within ~3ms, delta inside run-to-run noise.
All audit conclusions stand on the real build.

---

# ADDENDUM: transport reworked to the agents OTel endpoint (2026-06-10)

User directive: ADK runs must log to the AGENTS OTel endpoint
(/agents/otel/v1/traces) so they appear in the Weave Agents view — the
original calls transport (saveCallStart/End) only fed the Traces page. The
stack was rebuilt in place (same branch names/positions) with the plugin
emitting GenAI-semconv OTel spans through getWeaveTracer (shared provider,
src/genai/provider.ts) — same pipeline as piCodingAgent and the
openai-agents WeaveOtelTracingProcessor, same conventions as the Python
google_adk integration (weave/integrations/google_adk: extractors mirrored
incl. ADK_CAPTURE_MESSAGE_CONTENT_IN_SPANS gate, GOOGLE_GENAI_USE_VERTEXAI
provider naming, parts-model messages, tool definitions). Calls-era
versions of these slices: 91e4f038ee^..4fde323b1c history (notes on
c059413134 / ac1e328342 / d1cab0e03e / 4fde323b1c).

Key mechanics: spans parented via otelTrace.setSpan contexts; plugin
beforeExit hook registers BEFORE the provider's (listeners fire in
registration order — dangling spans must end before the provider flush);
tests inject InMemorySpanExporter via settings.genai.spanProcessor +
clearWeaveTracerProvider between tests; semconv.ts gained
gen_ai.agent.description/id + gen_ai.tool.definitions.

## Session addendum — rebase onto master
Rebased onto origin/master (ba337cff57), clean (no conflicts). Inherited branch 01's assertion shape-refactor + Settings-API migration. Verified 22/22 jest, prettier + eslint clean.

## Session addendum — use semconv constants
Inherited branch 01's conversion. Restack hit an import-block conflict (branch 01's added semconv import vs this branch's expanded googleAdk import adding commonPatchGoogleADK) — resolved keeping both. prettier rewrapped the merged googleAdk import. No new gen_ai literals in the auto-instrument test. Verified 22/22 jest, prettier + eslint clean.

## Session addendum — reparented onto split plugin-core
Reparented (cherry-pick) onto andrew/google-adk-05-agent-hierarchy after the plugin-core split. Source (googleAdk.ts module hooks, hooks.ts, examples/) + AGENTS.md applied cleanly. Test conflict resolved by taking the reorganized test + re-adding the commonPatchGoogleADK import and the auto-register test. Verified 24/24 jest + lint. Pre-split commit at tag bak-adk-old-auto (4efcea18c7).

---

## Session addendum — document the required ESM preload command
The user clarified that every PR in the stack must be standalone and that features/tests/docs should live in the PR that owns the behavior. Branch 06 owns automatic ADK instrumentation, so the ESM/tsx example commands and docblock needed to be corrected here. Static ESM imports load `@google/adk` before a normal `import 'weave'` module-body side effect can install the import hook, so the previous `npx tsx examples/googleAdkSmoke.ts` command did not demonstrate the auto-instrumentation path reliably. The correct standalone command preloads Weave first: `node --import=weave/instrument --import=tsx examples/googleAdkSmoke.ts`.

Changed files:
- sdks/node/src/integrations/googleAdk.ts: automatic usage docs now distinguish CJS `require('weave')` before ADK from ESM preload usage.
- sdks/node/examples/googleAdkSmoke.ts: run command now builds first and uses `node --import=weave/instrument --import=tsx` so the hook is active before the static ADK import evaluates.
- sdks/node/examples/googleAdkAgent.ts: example header now explains the preload-hook requirement and shows the corrected command.

Verification for this amendment:
- cd sdks/node && PATH=/opt/homebrew/opt/node/bin:$PATH pnpm test -- --runInBand src/__tests__/integrations/googleAdk.test.ts (25 passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec eslint src/integrations/googleAdk.ts src/integrations/googleAdk.types.ts src/integrations/googleGenAI.ts src/integrations/hooks.ts src/__tests__/integrations/googleAdk.test.ts examples/googleAdkAgent.ts examples/googleAdkSmoke.ts
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec prettier --check src/integrations/googleAdk.ts src/integrations/googleAdk.types.ts src/integrations/googleGenAI.ts src/integrations/hooks.ts src/__tests__/integrations/googleAdk.test.ts examples/googleAdkAgent.ts examples/googleAdkSmoke.ts
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run build
- PATH=/opt/homebrew/opt/node/bin:$PATH node --import=weave/instrument --import=tsx examples/googleAdkSmoke.ts (passed; emitted the known Node DEP0205 module.register deprecation warning, then verified auto-registration and agents-endpoint posts)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run lint (still fails on pre-existing consistent-type-imports errors in unchanged files: src/__tests__/clientMock.ts, src/clientApi.ts, src/integrations/openai-agents/messages.ts, src/state.ts, src/utils/urlToMediaPart.ts, src/weaveClient.ts)

No push performed.

---

## Session addendum - final branch-green verification after base lint cleanup
The user asked to run each PR in the stack and confirm it is green. Initial stack-owned checks passed, but full sdks/node npm run lint failed on every branch due the same @typescript-eslint/consistent-type-imports errors in unrelated files. A mechanical import-type cleanup was amended into the bottom branch so every surviving PR can pass full package lint. This branch was restacked after that base cleanup and then rerun.

Final verification for this branch:
- cd sdks/node && PATH=/opt/homebrew/opt/node/bin:$PATH pnpm test -- --runInBand src/__tests__/integrations/googleAdk.test.ts (25 passed, 25 total)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec eslint changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec prettier --check changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run build (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run lint (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH node --import=weave/instrument --import=tsx examples/googleAdkSmoke.ts (passed)
No push performed.

===================================================================
## ADDENDUM — type-precision pass (later session)
===================================================================

## Actions (slice 06 — auto-instrumentation)
No precision changes to slice-06-owned code. The lower-slice fixes were carried
up by restack. Reviewed and deliberately LEFT AS-IS:
- commonPatchGoogleADK(exports: any): matches the framework HookFn signature
  `(exports: any, name, baseDir) => any` and every peer integration
  (commonPatchGoogleGenAI/Anthropic/OpenAI). Tightening would fight HookFn.
- patchRunnerClass(RunnerClass: any): prototype-surgery seam; consistent with
  peer convention (no integration types the class it pulls off exports).
- globalSingleton<{plugin: WeaveAdkPlugin | null}>: the `null` models genuine
  lazy construction (plugin built on first runner use, not at module load).
- AdkRunnerLike.pluginManager?: optional because a patched runner-like object
  may not have it; checked with typeof guards at runtime.

## Verified (cumulative top of stack)
tsc esm+cjs PASS, prettier clean, eslint clean, jest 25/25. Pre-existing
typecheck:examples failures (OpenAI SDK drift in unrelated examples +
the untracked googleAdkWeaveCheck.ts scratch file) are NOT from this work —
confirmed identical on the pre-change backup tag.
