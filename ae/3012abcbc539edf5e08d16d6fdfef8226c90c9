---

## Session addendum — make full package lint green at stack base
After the stack was reshaped for standalone ownership, the user asked to run every PR and confirm it is green. The stack-owned checks passed on each branch, but `cd sdks/node && npm run lint` failed on every branch with the same `@typescript-eslint/consistent-type-imports` errors in files unrelated to the Google ADK integration. Since those failures would prevent a literal green result for every PR, applied the mechanical no-behavior `import type` cleanup at the bottom of the stack so all upstack PRs inherit it.

Changed files for this addendum:
- sdks/node/src/__tests__/clientMock.ts
- sdks/node/src/clientApi.ts
- sdks/node/src/integrations/openai-agents/messages.ts
- sdks/node/src/state.ts
- sdks/node/src/utils/urlToMediaPart.ts
- sdks/node/src/weaveClient.ts

This is intentionally a syntax-only lint cleanup: values that were only used as TypeScript types now use inline `type` imports. No runtime code paths changed.

Verification was rerun after this addendum across every surviving stack branch; see the later session summary/logs in .context/stack-green for exact branch-by-branch results. No push performed.

---

## Session addendum - final branch-green verification after base lint cleanup
The user asked to run each PR in the stack and confirm it is green. Initial stack-owned checks passed, but full sdks/node npm run lint failed on every branch due the same @typescript-eslint/consistent-type-imports errors in unrelated files. A mechanical import-type cleanup was amended into the bottom branch so every surviving PR can pass full package lint. This branch was restacked after that base cleanup and then rerun.

Final verification for this branch:
- cd sdks/node && PATH=/opt/homebrew/opt/node/bin:$PATH pnpm test -- --runInBand src/__tests__/integrations/googleAdk.test.ts (5 passed, 5 total)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec eslint changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec prettier --check changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run build (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run lint (passed)

No push performed.

===================================================================
## ADDENDUM — type-precision pass (later session)
===================================================================

## Conversation
User asked to scan the entire google-adk TS stack for type precision and fix:
(1) any/undefined/null that isn't actually true, (2) lazy `?` where upstream
is required, (3) over-defensive code that hurts readability. Verified every
duck type against the REAL @google/adk@1.2.0 + @google/genai .d.ts installed in
node_modules (ground truth), not from memory.

## Actions (slice 01 — owns the core duck types + run lifecycle + helpers)
googleAdk.types.ts:
- AdkPart.text: unknown -> string (genai Part.text is string | undefined; kept `?`)
- Removed dead fields never read by the impl: AdkPart.thought, AdkBlob.data,
  AdkEvent.id, AdkInvocationContext.branch, AdkCallbackContext.userContent.
- AdkEvent.content: dropped `| null` (genai/adk Content is never null, only undefined).
- AdkInvocationContext.agent/session -> required; AdkSession.id/appName/userId
  -> required (all required upstream; real types stay assignable to the duck
  types, verified by tsc against the test which drives the plugin with REAL
  @google/adk objects).
googleAdk.ts:
- toJsonSafe(): any -> unknown (all callers stringify / accept unknown).
- partToWire: String(part.text) -> part.text (text is now string).
- finishReasonString / contentToOutputMessage param: unknown ->
  `string | null | undefined`, dropped the now-redundant String() (FinishReason
  is a genai string enum).
- beforeRun/onEvent/afterRun: removed gratuitous `?.`/ternary guards on
  params.invocationContext (ADK-guaranteed) and ic.agent/ic.session (now
  required). Kept the empty-string `!invocationId` guard.

## Errors & Mistakes
- After dropping `?.` in onEventCallback the `invocations.get(...)` call became
  <80 cols; prettier wanted it on one line. Fixed here (it is slice-01 code) and
  re-restacked; that re-restack was conflict-free.

## Hints for Future Agents
- The duck types are deliberately structural (no instanceof). Making a field
  REQUIRED is safe iff the REAL upstream field is also required — a required
  real field is assignable to a required duck field. Do NOT make a field
  required when upstream has it optional.
- The test imports LlmRequest/LlmResponse from the REAL @google/adk, so tsc
  validates these duck types against reality. Trust tsc here.

## Verified
tsc esm+cjs PASS, prettier clean, slice-01 jest 5/5.
