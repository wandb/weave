## Conversation
Round 4 of the in-memory fake trace server stack. Rounds 1-3: built
SQLite-twin -> re-targeted to ClickHouse on review -> rebased onto #7153
(sqlite removal). This round, review feedback asked for: (a) PRs around
~500 lines ('1000 lines is definitely too much'), (b) pytest.mark.skipif
decorators instead of in-body 'if not client_is_clickhouse: pytest.skip'
gates, (c) merging the test-wiring PR (#7152, andrew/fake-trace-server)
into the skeleton PR (#7145).

## What changed structurally
- The module is now split into 17 stages (was 9); every PR diff is 265-624
  lines. Ordering note: the feedback APIs intentionally come AFTER the
  calls dynamic-field stage so the ClickHouse value-semantics helpers
  (JSON_VALUE/casts/compare) land with the calls engine — the fixpoint
  assigns module helpers to their FIRST USE, so stage order controls
  which PR carries shared machinery.
- Test wiring (conftest --trace-server=fake, util predicates, skipif
  gates, AGENTS.md) now rides the FIRST branch; #7152 is closed and the
  andrew/fake-trace-server branch deleted. The fake is selectable from
  PR 1; the suite only fully passes with --fake at the top of the stack.
- skipif conversion: in-body gates became
  @pytest.mark.skipif(NOT_CLICKHOUSE_BACKEND, reason=...) decorators.
  NOT_CLICKHOUSE_BACKEND (tests/trace/util.py) is a pytest condition
  STRING evaluated at collection time with config in the namespace —
  fixture-based predicates can't run there. It mirrors
  get_trace_server_flag precedence (--clickhouse > --fake >
  --trace-server). Fixture-internal skips (ch_server,
  calls_complete/otel/ttl internal_server) stay as pytest.skip — pytest
  has no fixture-level skipif. client_is_clickhouse(client) remains for
  the two genuinely mid-test branches (dupe_select partial assertion,
  call_stats force_merge helper).
- test_large_keys_are_stripped_call previously early-RETURNED on the fake
  (counted as passed); it is now an honest skipif (passed->skipped, totals
  unchanged).

## Verification on this exact tree
Fake: tests/trace_server 1203 passed / 115 skipped / 0 failed; tests/trace
1664 / 43 / 1 (test_flask_server, pre-existing env failure on every
backend). ClickHouse spot-verified after the skipif conversion: every
converted file runs with ZERO skips on a real ClickHouse backend
(batching/project_version/lifecycle/feedback 52 passed; annotation_queues/
queue_filtering/file_storage 71 passed, 0 skipped). Earlier full-CH runs
on the same tree (pre-refactor, semantics unchanged): 1317/1/0 and
1681/26/1 — per-shard totals match the fake exactly (1318, 1708).

## This commit (1/17: skeleton + wiring)
Module skeleton (records/storage/lifecycle) plus ALL test wiring in one
reviewable unit per review feedback. The 20-file diff is mostly
mechanical: conftest backend selection, NOT_CLICKHOUSE_BACKEND skipif
decorators across test files, fixture-skip restorations (#7153 had turned
them into asserts when ClickHouse was briefly the only backend), and
AGENTS.md docs.

## Hints for Future Agents
- skipif condition strings evaluate in the TEST MODULE namespace plus
  config/sys/os — import NOT_CLICKHOUSE_BACKEND (a plain str constant)
  and pass it positionally; no noqa needed because the import IS used.
- Do NOT replace the fixture-internal pytest.skip calls with asserts;
  --fake makes non-ClickHouse backends reachable again.
