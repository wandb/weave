## Conversation
Second half of 'implement the rest of those' — enable the deferred tests.

## Actions
Un-gated (removed skipif(FAKE_NOT_IMPLEMENTED) + now-unused imports) and verified
on --trace-server=fake:
- test_monitor_create_rejects/accepts (needs the feat-commit validation)
- test_refs_read_batch_missing_refs_reports_digests (needs feat-commit impl)
- test_agent_user_feedback (ALREADY worked — passed unchanged)
- test_filter_calls_by_ref_properties_with_table_rows_simple (ALREADY worked)
- test_declarative_eval_marks_child_calls_with_eval_meta + _merges_with_existing
  (ALREADY worked — _weave_eval_meta is set client-side by the Evaluation
  wrapper; the fake stores/returns call attributes faithfully)
Re-tagged FAKE_NOT_IMPLEMENTED -> NOT_CLICKHOUSE_BACKEND (permanent, not pending):
- test_lock_acquire_release_real_clickhouse (uses a real ClickHouse lock)
- test_flask_server (see below)

## Errors & Mistakes
- I assumed test_flask_server was backend-agnostic and un-gated it; it FAILED on
  the fake: httpcore.RemoteProtocolError 'Server disconnected without sending a
  response' + a SystemExit:1 thread exception. Root cause: the flask route calls
  weave.publish() inside a werkzeug request thread, where the in-process fake
  client is not reachable. A served backend (CH in tests) doesn't have this. So
  it's an architectural limit, not a missing fake capability -> NOT_CLICKHOUSE_BACKEND.

## Hints for Future Agents
- 3 of the 5 'portable gaps' were over-conservative skips, not real gaps. When a
  FAKE_NOT_IMPLEMENTED test looks portable, just un-gate and run it on the fake
  before writing any impl.
- FAKE_NOT_IMPLEMENTED now has ZERO skipif usages (only the doc comment in
  tests/trace/util.py). The alias is left defined for future capability gating.

## Codebase Discoveries
- FAKE_NOT_IMPLEMENTED == NOT_CLICKHOUSE_BACKEND (same condition string); the
  names encode intent (temporary-pending vs permanent-CH-only).

## Open Questions
- Whether to delete the now-unused FAKE_NOT_IMPLEMENTED alias from util.py.
