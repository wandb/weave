## Conversation
Part 7/8 of the fake trace server stack (split of the verified monolith;
full history in the note on backup/fake-ts-monolith).

## Actions
Segments: evaluate_model, rescore, evaluation_status, calls_score,
evaluation_run_*, prediction_*, score_*, eval_results_query. After this
commit the module is byte-identical to the verified implementation.

## Errors & Mistakes
None in this chunk.

## Dead Ends
None.

## Hints for Future Agents
- evaluate_model/rescore only dispatch through the injected
  EvaluateModelDispatcher (tests inject EvaluateModelTestDispatcher);
  rescore must NOT pre-create the call row (worker owns the lifecycle —
  there is a test asserting the dispatcher swap works via
  find_server_layer).
- score_create attaches a wandb.runnable feedback row to the prediction
  call — depends on part 4's feedback_create.
- eval_results_query delegates to the shared eval_results_helpers with
  this server's subtree-stream and table-row batch readers (parts 5 and 3).

## Codebase Discoveries
See the monolith note.

## Open Questions
None for this chunk.
