## Split of Google ADK plugin-core — slice 3/3 (tool/execute_tool spans)
Final slice. See slice 1 / tag bak-adk-old-plugincore for full original history.

Adds: tool callbacks (beforeTool/afterTool/onToolError → execute_tool spans, replacing slice 2's stubs), endToolSpan, syntheticQueueKey, InvocationState tool fields (toolSpans/syntheticToolKeys/toolSeq) + finishInvocation tool loop, tool types.

CRITICAL INVARIANT: this slice's googleAdk.ts + googleAdk.types.ts + semconv.ts are BYTE-IDENTICAL to the original plugin-core (tag bak-adk-old-plugincore) — verified 0-line diff. That is what lets the downstream branches (genai-exclusion/agent-hierarchy/auto-instrument) reparent onto this slice with their SOURCE applying cleanly; only the (reorganized) test file needed conflict resolution.

Tests: tool-using e2e (real runner) + tool-errors + cyclic/bigint sanitization. Verified 20/20 jest + lint clean.
