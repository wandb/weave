---

## Session addendum — make full package lint green at stack base
After the stack was reshaped for standalone ownership, the user asked to run every PR and confirm it is green. The stack-owned checks passed on each branch, but `cd sdks/node && npm run lint` failed on every branch with the same `@typescript-eslint/consistent-type-imports` errors in files unrelated to the Google ADK integration. Since those failures would prevent a literal green result for every PR, applied the mechanical no-behavior `import type` cleanup at the bottom of the stack so all upstack PRs inherit it.

Changed files for this addendum:
- sdks/node/src/__tests__/clientMock.ts
- sdks/node/src/clientApi.ts
- sdks/node/src/integrations/openai-agents/messages.ts
- sdks/node/src/state.ts
- sdks/node/src/utils/urlToMediaPart.ts
- sdks/node/src/weaveClient.ts

This is intentionally a syntax-only lint cleanup: values that were only used as TypeScript types now use inline `type` imports. No runtime code paths changed.

Verification was rerun after this addendum across every surviving stack branch; see the later session summary/logs in .context/stack-green for exact branch-by-branch results. No push performed.

---

## Session addendum - final branch-green verification after base lint cleanup
The user asked to run each PR in the stack and confirm it is green. Initial stack-owned checks passed, but full sdks/node npm run lint failed on every branch due the same @typescript-eslint/consistent-type-imports errors in unrelated files. A mechanical import-type cleanup was amended into the bottom branch so every surviving PR can pass full package lint. This branch was restacked after that base cleanup and then rerun.

Final verification for this branch:
- cd sdks/node && PATH=/opt/homebrew/opt/node/bin:$PATH pnpm test -- --runInBand src/__tests__/integrations/googleAdk.test.ts (5 passed, 5 total)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec eslint changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec prettier --check changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run build (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run lint (passed)

No push performed.
