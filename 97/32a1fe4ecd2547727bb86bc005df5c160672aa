## This commit
Rebuilt for round 10 (lint/type-check pass); see stack-wide context in
sibling commits' notes.

## Round 10 addendum: every PR now passes the repo lint (type checkers included)
User asked to 'ensure all PRs pass linting'. The repo's CI lint gate
(nox -e lint -> prek pre-push hooks) effectively runs THREE always_run
type checkers repo-wide — ty, pyright@1.1.387 (relaxed: attr/call/arg
checks off, tests excluded), and mypy@1.19.1 (tests excluded, weave.*
has disallow_untyped_defs/calls) — while the ruff hooks only see staged
files. So 'passing lint' per PR means every intermediate module state
must type-check.

Found and fixed two classes of problems:
1. Stage-ordering forward references: _compile_expanded_field /
   _resolve_ref_str_for_filter / _expand_refs lived in the late
   refs-threads stage but were called from the compile/read stages —
   mypy attr-defined errors on 5 intermediate branches (the Protocol base
   covers PUBLIC methods, so only private helpers can dangle). Fixed by
   moving them to their first callers; _compile_calls_sort_field moved
   into the compilation stage to keep the read stage in size budget.
   New deltas: compile 573, read 595, eval-subtree+threads 179.
2. 16 genuine mypy errors in the canonical module (never caught before
   because an earlier scoped mypy run had silently executed against the
   skeleton branch — the gt-track HEAD footgun AGAIN): Optional
   started_at/ended_at flow from the part-merge change (latency, threads
   aggregation — restructured with explicit non-None narrowing),
   calls_delete re-binding a comprehension-typed name to an Optional,
   feedback_aggregate narrowing time_bucket_seconds and reusing 'key'
   for both a list and tuple, _log_completion_span response now
   dict | None (open stream spans), _ch_sorted_by_terms terms param
   widened to Sequence (invariance), _ch_position bool() coercion,
   _orm_select 'out' no-redef rename.

## Verification (round 10)
- mypy@1.19.1 scoped: 0 errors on ALL 17 stage states (loop-verified
  with each stage written into the tree).
- ty: clean on full repo and spot stages; pyright@1.1.387: 0 errors on
  the canonical module; ruff: all stages + tree clean.
- Suites unchanged: fake tests/trace_server 1203/115/0; tests/trace
  1664/44/1 (flask, pre-existing).
- Static per-stage guard added to the process: every self._private call
  must resolve within its stage (now 0 issues; checked in the splitter
  workflow).

## Hints for Future Agents
- The CI lint job runs 'prek run --hook-stage=pre-push' WITHOUT
  --all-files: only always_run hooks (the type checkers) actually gate
  PRs; ruff runs against staged files (no-op in CI). Don't assume
  ruff-clean == lint-clean.
- mypy narrowing does not survive list comprehensions or closures over
  Optionals; the threads/feedback_aggregate restructures exist for the
  type checker, keep the explicit is-not-None filters.

## ADDENDUM: addressed 4 PR-review comments (#7145)
1. test.yaml (@drtangible): stale "tests/fake_unimplemented.txt" ref (file never existed; design uses per-test skipif decorators) -> comment corrected.
2. util.py (@gtarpenning): FAKE_NOT_IMPLEMENTED copy-pasted NOT_CLICKHOUSE_BACKEND's condition -> aliased `FAKE_NOT_IMPLEMENTED = NOT_CLICKHOUSE_BACKEND` (distinct name kept; explanatory comment kept).
3. _calls keying (@gtarpenning): re-keyed self._calls from {id: rec} to {(project_id, id): rec} to mirror ClickHouse scoping. Applied consistently down the whole stack via a deterministic idempotent transform run at every commit (git rebase d69777541a --exec /tmp/fix_all.py). Writes use no-paren subscript self._calls[rec.project_id, rec.id] (RUF031); reads use .get((project_id, id)) with project_id from req.start/req.end/req.project_id; .values() iterations unchanged (already filter rec.project_id). The redundant `rec.project_id == req.project_id` check at annotation_queue_add_calls is now always-true but left in place (harmless, minimal diff).
4. subclassing FullTraceServerInterface (@gtarpenning): KEPT. It's a Protocol; ClickHouseTraceServer subclasses it identically; inheriting makes pyright/mypy verify the full interface is implemented. No change.
Verified: re-key correct on fake (test_client_trace 124p + annotation_queues 57p, 177 total post-rebase); ruff check+format clean on all 17; consistency sweep old-key=0/new-decl=1 across the stack.

## ADDENDUM: rebased onto origin/master e8c9cb6662 (+13 commits)
Conflicts (master changed files the stack gates):
- test_base_object_classes.py: master refactored monitor tests to a `_invalid_field_message` helper (tests still exist). Merged master's refactor + the stack's monitor gating; monitor pair stays gated on the fake.
- test_feedback.py: master ADDED test_agent_user_feedback. Gated on the fake (conservative, like monitor-accepts); it PASSES on the fake once feedback lands (verified), so it could be un-gated at PR8 (sibling test_agent_monitor_feedback_filters un-gates there) — left gated per push-as-is.
- test_llm_as_a_judge_scorer.py: import merge (master's Scorer + FAKE); FAKE removed at the PR4 un-gate.
The recurring FAKE-import add/remove churn at un-gate commits was auto-resolved by /tmp/resolve_imports.py (defers non-import conflicts to manual).
TRAP for future agents: a NEW master test in a file the stack FULLY un-gates creates an import-persistence bug — the still-gated new test needs the FAKE import, but the un-gate's ruff --fix removes it as F401 -> NameError. Fix: a rebase --exec re-adding the FAKE import wherever a FAKE skipif remains (idempotent), OR un-gate the new test where its feature lands.
Verified: ruff check+format clean on all 17; conflict files + call CRUD pass/skip on the fake (168 passed, 10 skipped); PR1 descends origin/master.
