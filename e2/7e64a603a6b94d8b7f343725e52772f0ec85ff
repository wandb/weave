## Conversation
Slice 3/4 of the REFRAMED Google ADK stack (each PR standalone + tested + docs). This slice adds what slice 1 deliberately left out: invoke_agent synthesis. See slice 1 note (andrew/google-adk-01-plugin-core) for reframe context; the original research notes live on the old andrew/adk-ts-01-plugin commit de553f7b46.

## Actions
Built by restore-then-trim: restored googleAdk.ts/types/tests from the old stack final (andrew/adk-ts-03-tests), re-applied the \\0-escape fix for the syntheticQueueKey separator (the old blob has a literal NUL byte), then removed ONLY the auto-instrumentation parts (module-hook section, its imports globalSingleton/addCJS+ESMInstrumentation/AdkRunnerLike, the ADK_VERSION_RANGE/ADK_CJS_SUBPATH consts, the Automatic-usage header block, AdkPluginManager/AdkRunnerLike types, the auto-registration test and its commonPatchGoogleADK import). Diff vs slice 1 therefore reads as pure hierarchy: state fields (rootAgent/agentCalls/agentCallOrder/openToolKeys), findAgentInTree, ensureAgentCall + innermostOpenToolCall, agent callbacks implemented, reparenting, finishInvocation agent loop, test tightening. AGENTS.md gotchas bullet gains the agent-callback-gap sentence (final wording).

## Errors & Mistakes
The first excision of the auto-registration test left a stray blank line before the describe close (`    });\n\n  });`); fixed so slice 4 restores the file to the exact old-stack content without whitespace noise.

## Dead Ends
None in this slice.

## Hints for Future Agents
- The single-agent test's parenting assertions were intentionally RE-TIGHTENED here from slice 1's flat expectations (parent = root) to the hierarchy (parent = invoke_agent). That is a deliberate behavior change in the stack, not assertion weakening.
- ensureAgentCall's MAX_AGENT_ANCESTRY_DEPTH guards parentAgent cycles; out-of-tree fallback order is innermost open tool call, then root.

## Codebase Discoveries
None new (see prior slices).

## Open Questions
None.

---

# ADDENDUM: transport reworked to the agents OTel endpoint (2026-06-10)

User directive: ADK runs must log to the AGENTS OTel endpoint (/agents/otel/v1/traces) so they appear in the Weave Agents view — the original calls transport (saveCallStart/End) only fed the Traces page. The stack was rebuilt in place (same branch names/positions) with the plugin emitting GenAI-semconv OTel spans through getWeaveTracer (shared provider, src/genai/provider.ts) — same pipeline as piCodingAgent and the openai-agents WeaveOtelTracingProcessor, same conventions as the Python google_adk integration (weave/integrations/google_adk: extractors mirrored incl. ADK_CAPTURE_MESSAGE_CONTENT_IN_SPANS gate, GOOGLE_GENAI_USE_VERTEXAI provider naming, parts-model messages, tool definitions). Calls-era versions of these slices: 91e4f038ee^..4fde323b1c history (notes on c059413134 / ac1e328342 / d1cab0e03e / 4fde323b1c).

Key mechanics: spans parented via otelTrace.setSpan contexts; plugin beforeExit hook registers BEFORE the provider's (listeners fire in registration order — dangling spans must end before the provider flush); tests inject InMemorySpanExporter via settings.genai.spanProcessor + clearWeaveTracerProvider between tests; semconv.ts gained gen_ai.agent.description/id + gen_ai.tool.definitions.

## Session addendum — rebase onto master
Rebased onto origin/master (ba337cff57). Restack hit a conflict in test-1's "root carries the user message" block: branch 01 consolidated it to `expect(root.attributes).toMatchObject({...})`, while this branch had changed `root` to come from `.find()` (hence `root!`). Resolved by merging both into `expect(root!.attributes).toMatchObject({...})`. Inherited branch 01's other assertion consolidations + Settings migration. Verified 21/21 jest, prettier + eslint clean.

## Session addendum — use semconv constants
Inherited branch 01's conversion; additionally converted this branch's own 'gen_ai.agent.name' literals (in the sub-agent tests) to ATTR_GEN_AI_AGENT_NAME. Restack hit a test-1 conflict (my constant conversion vs this branch's per-agent root!/agentSpan structure) — resolved keeping both (structure + constants). prettier rewrapped two now-longer lines. Verified 21/21 jest, prettier + eslint clean.

## Session addendum — reparented onto split plugin-core
Reparented (cherry-pick) onto andrew/google-adk-04-genai-exclusion after the plugin-core split. googleAdk.ts/types applied cleanly (split slice-3 source == old plugin-core). Test conflict resolved by taking the new reorganized test structure + re-applying this branch's two logical changes: the single-agent e2e 1→2-span hierarchy modification, and the two sub-agent tests (nests-sub-agents, agents-outside-tree). Verified 23/23 jest + lint. Pre-split commit at tag bak-adk-old-agent (3c457ed993).

## Session addendum — keep inherited runner test valid under hierarchy
The user clarified that every PR in the Google ADK stack must be standalone, with features and tests moved to the PR that owns their behavior. Branch 05 is where per-agent `invoke_agent` spans are introduced, so the real ADK runner smoke test inherited from branch 01 had to be adjusted here rather than downstack. Once hierarchy exists, the test file can contain multiple `invoke_agent` spans; selecting the first by operation is order-dependent and can bind to a child/per-agent span instead of the root invocation. Updated only this branch to select the `invoke_agent` span with no parent span id for the inherited explicit-plugin smoke assertion.

Verification for this amendment:
- cd sdks/node && PATH=/opt/homebrew/opt/node/bin:$PATH pnpm test -- --runInBand src/__tests__/integrations/googleAdk.test.ts (24 passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec eslint src/integrations/googleAdk.ts src/integrations/googleAdk.types.ts src/integrations/googleGenAI.ts src/__tests__/integrations/googleAdk.test.ts
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec prettier --check src/integrations/googleAdk.ts src/integrations/googleAdk.types.ts src/integrations/googleGenAI.ts src/__tests__/integrations/googleAdk.test.ts

No push performed.

---

## Session addendum - final branch-green verification after base lint cleanup
The user asked to run each PR in the stack and confirm it is green. Initial stack-owned checks passed, but full sdks/node npm run lint failed on every branch due the same @typescript-eslint/consistent-type-imports errors in unrelated files. A mechanical import-type cleanup was amended into the bottom branch so every surviving PR can pass full package lint. This branch was restacked after that base cleanup and then rerun.

Final verification for this branch:
- cd sdks/node && PATH=/opt/homebrew/opt/node/bin:$PATH pnpm test -- --runInBand src/__tests__/integrations/googleAdk.test.ts (24 passed, 24 total)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec eslint changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH pnpm exec prettier --check changed TypeScript files (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run build (passed)
- PATH=/opt/homebrew/opt/node/bin:$PATH npm run lint (passed)

No push performed.

===================================================================
## ADDENDUM — type-precision pass (later session)
===================================================================

## Actions (slice 05 — owns per-agent span synthesis)
googleAdk.ts:
- before/afterAgentCallback: dropped `?.` on ADK-guaranteed callbackContext and
  agent (agent: BaseAgent, callbackContext: Context are required).
- InvocationState.rootAgent: `AdkBaseAgent | null` -> `AdkBaseAgent`. It is set
  only in beforeRunCallback from `ic.agent`, which is now required (slice 01),
  so it is never null.
- ensureAgentSpan: removed the now-dead `if (state.rootAgent)` guard around
  findAgentInTree; `let agent` -> `const agent = findAgentInTree(...)`. agent can
  still be undefined (out-of-tree agents), so the rest of the parent-resolution
  logic is unchanged.

## Hints for Future Agents
- findAgentInTree returning undefined (out-of-tree agent, e.g. AgentTool) is a
  DIFFERENT nullability from rootAgent — that path (innermostOpenToolSpan
  fallback) is still live and intentional. Don't conflate the two.

## Verified
tsc esm+cjs PASS, prettier clean, slice-05 jest 24/24.

---
## Session addendum — review-comment pass (2026-06-23)

Comment 2 (toMatchObject): converted the lone agentSpan AGENT_NAME assert to
expect(agentSpan!.attributes).toMatchObject({[ATTR_GEN_AI_AGENT_NAME]:
'weather_agent'}). Converted here (not slice 03) because slice 05 is where this
assertion reaches its final agentSpan form (slice 03 asserted on `root`).

This slice's restack also absorbed slice 03's loop->toMatchObject conversion via
conflict resolution (kept slice 05's root! + slice 03's toMatchObject); no
behavior change. Validated at stack tip: 26 ADK jest tests pass, typecheck clean.

---
## Session addendum — literal OTEL keys in tests (2026-06-23)

Per review feedback ("use the literal OTEL key instead of the const ... easier
to read in the test"), googleAdk.test.ts now uses literal OTEL semconv strings
(e.g. 'gen_ai.output.messages', 'gen_ai.tool.call.id') in place of the
ATTR_GEN_AI_* consts imported from ../../genai/semconv; the now-unused semconv
import was removed. TEST-ONLY — production googleAdk.ts still imports the consts.

Applied uniformly across the whole ADK slice stack (01/02/03/05/06) via a
deterministic, idempotent transform (.context/relit.mjs, gitignored): object
keys [CONST]: -> 'lit':, property access x[CONST] -> x['lit']. Each upstack
restack hit the test file and was resolved with `git checkout --theirs` +
re-running the transform, so every slice's full file ends up literal-ized.
Notes survived because these restacks took git's conflict path (gt's
clean-restack path would have dropped them).

Validated: eslint clean, typecheck:esm clean; jest green (tip 26, slice 03 22,
slice 01 5).

---
## Session addendum — review pass: drop ! (2026-06-23)

Comment 1: every `.find()`-derived span in this slice now goes through the
`requireSpan` helper, dropping all `!` non-null assertions and the redundant
`expect(x).toBeDefined()` lines — runner_agent root (slice 05 switched this test
to `.find()` for the hierarchy), tool/hierarchy root+agentSpan, nests-sub-agents
root/pipeline/first/second (via the `byAgent` closure, now requireSpan-wrapped),
and outside_agent. Validated: jest 24 pass.

---
## Session addendum — review pass: doc reword + drop redundant order-arrays (2026-06-23)

Three review questions on this slice's googleAdk.ts; user said reword #3, fix #5/#6.

Comment 3 (line ~505): reworded findAgentInTree's doc from "without trusting ADK
methods" to spell out the behavior — uses ADK's own `findAgent` when present,
falls back to a manual BFS walk otherwise (findAgent is optional + may throw).

Comments 5 & 6 (iteration): removed the redundant `openToolKeys` and
`agentSpanOrder` string[] order-arrays from InvocationState. They duplicated the
insertion order of `toolSpans` / `agentSpans` (JS Maps preserve insertion order)
and were pruned in lockstep with them (endToolSpan deleted both; the after-agent
path deleted both), so they carried no information the Maps didn't already have.
  - innermostOpenToolSpan now iterates `toolSpans.values()` keeping the last
    (insertion-ordered + pruned-on-close ⇒ last open span = innermost).
  - finishInvocation ends agent spans via `[...agentSpans.values()].reverse()`
    (children-first), matching the old reverse-order-array loop.
  - Folded the ordering rationale into the agentSpans / toolSpans field docs.
Removed all 7+7 maintenance sites (field/init/push/filter/clear/read). Tool
cleanup already iterated toolSpans.values() directly, so this also makes the
file internally consistent.

TEST behavior unchanged (no `!`/key indirection in source affects spans).
Validated: eslint + typecheck:esm clean; jest slice 05 24, tip 26. Slice 06
inherits via restack (0 references to the removed fields). Kept LOCAL/unpushed.

---
## Session addendum — review pass: simplify doc comments (2026-06-23)

Doc-only changes to googleAdk.ts (no behavior change), per review comments:
- Reworded the "Agent nesting" module-docstring paragraph into plainer English
  (dropped the inline method names; same meaning).
- Trimmed the `agentSpans` field doc to one line; dropped "ended in reverse" —
  that rationale already lives in finishInvocation's inline comment.
- Trimmed the `toolSpans` field doc to one line; dropped the insertion-ordering
  note. The "innermost = last" mechanic is in innermostOpenToolSpan's comment,
  and the "out-of-tree agent / AgentTool fallback parent" rationale is at the
  caller (the `if (!agent ...)` branch).

A 4th comment asked why findAgentInTree needs the manual-walk fallback — that
was a question; answered in chat (findAgent is optional in our duck type + may
throw; manual BFS keeps span-parenting working; caller degrades to
innermostOpenToolSpan/rootSpan), left the code as-is pending the user's call.

Validated: eslint + typecheck:esm clean, jest slice 05 24. Kept LOCAL/unpushed.

---
## Session addendum — review pass (2026-06-23)

No own edits — inherits slice 03's KNOWN LIMITATION comment relocation and slice
06's docstring reword via restack. Validated at tip: jest 26. Kept LOCAL/unpushed.

===================================================================
## ADDENDUM — duck-type file deleted; migrate to provided types (later session)
===================================================================

## Conversation
Reviewer @drtangible left a GitHub comment on PR #7301 against
sdks/node/src/integrations/googleAdk.types.ts (lines 5-6): "This file no longer
exists [in the openai pattern], and we've started moving towards using provided
types where possible. Guessing we could do the same with this library, assuming
they export what we need." The user directed: rebase onto origin/master, prefer
the types ADK exports, keep local types only for shapes it does NOT export, and
follow how the OpenAI integration does it (openai.agent.ts / openai-agents/
import provided types directly and have NO companion *.types.ts duck-type file).
User then clarified "follow what Dan suggested" = option B: DELETE
googleAdk.types.ts entirely.

## Actions
1. Rebased the whole 5-slice stack onto origin/master 841d7f879e (gt sync
   --no-restack FF'd the worktree-shared master, then gt restack the slices).
2. Deleted sdks/node/src/integrations/googleAdk.types.ts in every slice.
3. In googleAdk.ts, replaced `import type {Adk*} from './googleAdk.types'` with
   provided types, aliased to keep the Adk* names and dodge global collisions
   (Event/Content/Part/Blob):
     - from '@google/adk':   BaseAgent, BaseTool, Event, InvocationContext,
                             LlmRequest, LlmResponse   (as Adk*)
     - from '@google/genai': Content, FunctionDeclaration,
                             GenerateContentConfig, Part   (as Adk*)
   Verified against the installed .d.ts that @google/adk exports the ADK types
   but does NOT re-export the genai types, so the @google/genai import is
   genuinely required (can't fold everything into one @google/adk import).
4. Inlined the shapes neither package exports, as local interfaces with
   docstrings explaining why: AdkCallbackContext / AdkToolContext (invocationId
   & agentName are getters on ADK's non-exported ReadonlyContext; ToolContext
   adds functionCallId), AdkPluginManager / AdkRunnerLike (Runner/PluginManager
   internals the hook touches). Each slice carries only the gaps it uses.
5. Tests: the partial mock object literals the tests pass to plugin callbacks no
   longer satisfy the strict provided types, so added `as any` to the
   event/llmRequest/tool/toolContext mocks (test-only; behavior unchanged).
6. The rebased master tightened eslint (@typescript-eslint/no-require-imports);
   converted the test's setGlobalClient `require` to a top-level import.

Migration was applied deterministically: .context/migrate-adk-types.mjs (import
rewrite + gap-interface emission + file delete) and .context/cast-mocks.mjs
(brace-matched `as any` on mock literals), cascaded bottom-up via gt restack
with take-theirs conflict resolution per slice.

## Validation (this slice, independent)
- src/integrations/googleAdk.types.ts gone; googleAdk.ts imports provided types
- npm run typecheck:esm: clean
- eslint googleAdk.ts + googleAdk.test.ts: clean
- jest googleAdk.test.ts: all passing

## Hints for Future Agents
- `import type` is erased at runtime, so @google/genai resolves transitively
  through @google/adk; jest/runtime never need it and typecheck passes because
  it is flat in node_modules. DEFERRED (flagged to user, intentionally NOT
  done): promoting @google/genai to a direct devDep for parity with how
  openai-agents declares both @openai/agents and openai. Held back to avoid
  noisy package-lock churn in the requested push; it is a clean 1-line
  package.json + lockfile follow-up. NB @google/adk@^1.2.0 is already on master
  (no ADK slice touches package.json).
- gt's clean-restack path DROPS git notes; these notes were recovered from the
  pre-migration SHAs via reflog and `git notes copy -f <old> <new>`, then this
  addendum appended.

## Errors & Mistakes
- migrate-adk-types.mjs first used a greedy `[\s\S]*?` regex that matched from
  the FIRST `import type {` (the @opentelemetry block), mangling unrelated
  imports and dropping AdkBaseAgent. Fixed to
  `import type \{([^}]*)\} from '\./googleAdk\.types';` ([^}]* can't cross
  another import's braces). Caught only because source typecheck was rerun —
  jest alone would have missed it; always run typecheck, not just tests.
- cast-mocks.mjs missed `const tool = {...}` variable mocks (it matched only
  `tool: {`); handled those by hand.

## Addendum — normalize import order (externals grouped at top)
Reviewer flagged that the @google/adk import block "moved" between slices (churn
between PRs) and asked to check all import moves. Audit found two inconsistencies
at the 01→02 boundary, both now fixed to ONE convention — external imports
(@opentelemetry, @google/adk, @google/genai, zod) grouped at the TOP, relative
imports below — in BOTH googleAdk.ts and googleAdk.test.ts, every slice:
- SOURCE: slice 01 had @google/* at the bottom (where the old ./googleAdk.types
  relative import used to sit before the type migration); moved to the top to
  match 02-06.
- TEST: slices 02-06 had @google/adk at the bottom; moved to the top to match
  slice 01.
No linter enforces import order here (there is no import/order rule, and
`eslint --fix` is a no-op on imports), so this is purely churn-reduction for
cleaner per-PR diffs — not a lint fix.

Restack mechanics: slice 01 amended (source only); 02-06 restacked. Conflicts hit
on 02 (symbol-set merge: slice 02 adds LlmRequest/LlmResponse/FunctionDeclaration/
GenerateContentConfig) and on 06 (the test import block tangled into DUPLICATE
relative imports via git's auto-merge — clientApi appeared 3x). Both resolved to
the canonical layout. 03/05 auto-merged clean (verified not tangled). Confirmed
clientApi imported exactly once per test file and all slices green
(typecheck/eslint/jest 5/19/22/24/26).

GOTCHA for future agents: reordering imports across a stacked PR is conflict-prone
— moving a block in a lower slice collides with later slices that add symbols to
it or add neighboring imports, and a "clean" auto-merge can SILENTLY duplicate
imports (slice 06 here). Always re-validate each slice (typecheck + a dup-count
grep), don't trust a clean restack.

## Addendum — rebased onto origin/master 58285d4c6b
Restacked the whole stack onto latest origin/master (was 841d7f879e). The 3 new commits are Python-only (eval predict-token-total #7364, trace-server owns AgentSpanOpName #7357, prompt/type_wrappers decouple) — zero sdks/node overlap, so the rebase was conflict-free and slice content is unchanged (re-parent only). Tip green: typecheck/eslint/jest 26/26.
