## Conversation
Phase-1 PR4 (final) of the import-linter decoupling stack. User: vendor what you need, no shared dir, don't codegen, leave codegen-destined files untouched. I earlier asked whether to handle the core-repo side in lockstep or land shims; user said 'go for phase 1' so I chose the safe shim path (no core access from this workspace).

## Actions
- Target ignores: 7 (Contract 1: isolated_client_executor x2 + evaluate_model_worker x5) + 5 (Contract 3: the same worker x5; Contract 3 dedups against Contract 1).
- Verified the server depends on the worker ONLY via the EvaluateModelDispatcher ABC (clickhouse_trace_server_batched.py:324, in_memory_trace_server.py:153). No string/mock.patch/importlib refs to the modules (grepped).
- Created weave/eval_worker/ (package, outside both contracts). git mv'd isolated_client_executor.py and evaluate_model_worker.py into it (preserves blame).
- Created weave/trace_server/eval_model_dispatcher.py holding the EvaluateModelDispatcher ABC (client-free; imports EvaluateModelArgs from trace_server_interface). Removed the ABC from the moved worker; it now imports + re-exports it.
- Repointed clickhouse + in_memory server imports to eval_model_dispatcher.
- Left thin re-export shims at both OLD paths for core back-compat (public names only).
- Repointed 4 in-repo test files to weave.eval_worker.* (shims are for core, not in-repo tests).
- pyproject: removed the 7 Contract-1 entries (leaving only the 4 llm_structured_model), emptied Contract-3's list (ignore_imports = []), and added a header note that weave.eval_worker is intentionally outside the contracts.

## Errors & Mistakes
- First test run failed: conftest.py imports the PRIVATE _worker_loop, which my minimal shim (public names only) didn't re-export. Fix: repoint the in-repo tests to weave.eval_worker (correct anyway) rather than fattening the shim with a private name.
- ruff I001 unsorted-imports on clickhouse/in_memory and the 4 test files (the new import needed reordering); ruff --fix resolved all.

## Verification
- lint-imports: 3 kept / 0 broken; analyzed files 441 -> 451 (new package). 12 ignores removed.
- ruff: clean on all changed prod + test files.
- runtime: EvaluateModelDispatcher identity is the SAME object via eval_model_dispatcher, eval_worker.evaluate_model_worker, and the old shim path; IsolatedClientExecutor identity same via shim + new path.
- tests: tests/trace_server/isolated_client_executor/ + test_trace_server_evaluation_apis.py = 40 passed on --trace-server=fake. Full tests/trace_server collects 1379, no import errors.

## Dead Ends
Considered fattening the isolated shim to re-export _worker_loop (zero test churn). Rejected: a back-compat shim should expose only the public API; in-repo tests should reference the real (moved) location.

## Hints for Future Agents
- CROSS-REPO: the core repo (services/weave-trace) imports evaluate_model + EvaluateModelArgs from weave.trace_server.workers.evaluate_model_worker.evaluate_model_worker and IsolatedClientExecutor from weave.trace_server.isolated_client_executor. The shims keep that working. The weave->core mirror CI (see memory weave_mirror_core_ci) would break without them. To finish the decoupling: repoint core to weave.eval_worker.* and weave.trace_server.eval_model_dispatcher, then delete the shims.
- The shims live UNDER weave/trace_server/ and transitively import the client via weave.eval_worker. lint-imports allows this because allow_indirect_imports=true checks DIRECT edges only (shim's direct import is weave.eval_worker, not a forbidden module). The server's OWN production code (clickhouse/in_memory) imports the clean eval_model_dispatcher seam, so the extraction goal holds.
- EvaluateModelArgs canonical home stays weave.trace_server.trace_server_interface; EvaluateModelDispatcher canonical home is now weave.trace_server.eval_model_dispatcher.
- production dispatch also passes RescoringArgs through the same .dispatch() seam (EvalWorkerJob union); the ABC annotation says EvaluateModelArgs (narrower than reality) — left as-is to minimize churn.

## Codebase Discoveries
- isolated_client_executor.py has ZERO production importers in weave/ (only tests + core); it is shipped here but consumed at runtime by core.
- The isolated_client_executor tests run real subprocesses; conftest monkeypatches IsolatedClientExecutor._ensure_process_running to use fork (not spawn) for speed.

## Open Questions
- Should the isolated_client_executor + workers test dirs eventually move to tests/eval_worker/ to mirror the source? Left in place to minimize churn this PR.
- The 4 llm_structured_model ignores remain (codegen-destined); they + the builtin registry are Phase 2.
